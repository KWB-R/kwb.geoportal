% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_wfs.R
\name{find_wfs}
\alias{find_wfs}
\title{Find WFS services in the Geoportal catalogue}
\usage{
find_wfs(pattern = NULL, metadata = NULL, protocol = "OGC:WFS", ...)
}
\arguments{
\item{pattern}{regular expression matched, case-insensitively, against the
record title, its abstract and the link URL. \code{NULL} (default) returns
every WFS link.}

\item{metadata}{result of a previous \code{\link[=read_metadata_all]{read_metadata_all()}} call. Pass this
to search repeatedly without downloading the catalogue again. If \code{NULL}
(default) the catalogue is read.}

\item{protocol}{link protocol to keep (default: \code{"OGC:WFS"}). Pass
\code{"OGC:WMS"} to find map services instead, or \code{NULL} for all of them.}

\item{...}{further arguments passed to \code{\link[=read_metadata_all]{read_metadata_all()}}, e.g.
\code{base_url}.}
}
\value{
tibble with columns \code{title}, \code{service}, \code{link_url}, \code{link_desc},
\code{link_protocol}, \code{geonet_uuid}, sorted by title. Warns, and returns no
rows, when \code{metadata} holds no links at all, which is a different thing
from \code{pattern} not matching.
}
\description{
Searches the GeoNetwork catalogue read by \code{\link[=read_metadata_all]{read_metadata_all()}} for records
that offer a download service, and returns one row per matching WFS link.
The \code{service} column of the result is what \code{\link[=read_wfs]{read_wfs()}} takes as its first
argument.
}
\details{
This answers "which endpoint belongs to dataset X". It is a convenience for
discovery: once the service name is known it should be written down in the
calling code, so that fetching the data no longer depends on the catalogue
being reachable.
}
\examples{
\dontrun{
catalogue <- read_metadata_all()
find_wfs("kanalisation", metadata = catalogue)
find_wfs("alkis", metadata = catalogue)
}
}
