% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_wfs.R
\name{read_wfs}
\alias{read_wfs}
\title{Read a WFS layer from the Berlin Geoportal}
\usage{
read_wfs(
  service,
  layer = NULL,
  srs = "EPSG:25833",
  version = c("2.0.0", "1.1.0", "1.0.0"),
  output_format = "application/json",
  count = NULL,
  start_index = NULL,
  host = "https://gdi.berlin.de/services/wfs",
  quiet = FALSE
)
}
\arguments{
\item{service}{service name, see \code{\link[=wfs_base_url]{wfs_base_url()}}, or a full endpoint URL.}

\item{layer}{feature type to request. Either the fully qualified name
(e.g. \code{"wsg:wsg"}) or the bare layer name, in which case the service name
is used as the namespace prefix. If \code{NULL} (default) the service is asked
for its feature types via \code{\link[=list_wfs_layers]{list_wfs_layers()}}; this only succeeds when the
service offers exactly one.}

\item{srs}{coordinate reference system of the response (default:
\code{"EPSG:25833"}, the ETRS89 / UTM 33N used by the Berlin services).}

\item{version}{WFS version, one of \code{"2.0.0"} (default), \code{"1.1.0"}, \code{"1.0.0"}.}

\item{output_format}{value of \code{OUTPUTFORMAT} (default:
\code{"application/json"}, i.e. GeoJSON). Pass \code{NULL} to let the server choose,
which usually yields GML.}

\item{count}{maximum number of features to return, or \code{NULL} (default) for
all of them. Sent as \code{COUNT} for WFS 2.0.0 and as \code{MAXFEATURES} otherwise.}

\item{start_index}{index of the first feature to return, or \code{NULL}
(default). Together with \code{count} this allows paging through layers that
exceed the server's response limit. WFS 2.0.0 only.}

\item{host}{see \code{\link[=wfs_base_url]{wfs_base_url()}}.}

\item{quiet}{suppress the progress messages of \code{\link[sf:read_sf]{sf::read_sf()}} and of this
function (default: \code{FALSE}).}
}
\value{
an \code{sf} data frame.
}
\description{
Downloads one feature type of a WFS service and reads it into an \code{sf}
object. This is the data counterpart to \code{\link[=read_metadata]{read_metadata()}}, which only
describes where a dataset lives.
}
\details{
The request is a plain \code{GetFeature} call against
\verb{<host>/<service>?SERVICE=WFS&VERSION=...&REQUEST=GetFeature&TYPENAMES=...},
which is the form the Berlin endpoints answer.
}
\examples{
\dontrun{
# Water protection zones, the layer the model's dashboard uses
wsg <- read_wfs("wsg", "wsg")

# ATKIS river centrelines
rivers <- read_wfs("atkis", "b17_ax_gewaesserstationierungsachse_l")
}
}
