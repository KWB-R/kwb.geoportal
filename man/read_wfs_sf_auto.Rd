% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_wfs_sf_auto.R
\name{read_wfs_sf_auto}
\alias{read_wfs_sf_auto}
\title{Read a WFS layer (auto-detect from URL)}
\usage{
read_wfs_sf_auto(url, crs_target = 25833, bbox = NULL, quiet = FALSE)
}
\arguments{
\item{url}{character(1). Full WFS URL (e.g. a GetCapabilities link).}

\item{crs_target}{integer(1) or \code{NULL}. EPSG code to reproject the result to.
Defaults to \code{25833} (ETRS89 / UTM zone 33N). Use \code{NULL} to keep native CRS.}

\item{bbox}{numeric(4) or \code{NULL}. Optional bounding box
(\code{c(xmin, ymin, xmax, ymax)}), passed to the GetFeature request.}

\item{quiet}{logical(1). Suppress download messages (default: \code{FALSE}).}
}
\value{
An \code{sf} object with the requested WFS layer.
}
\description{
Simplified helper to read a single WFS layer into R using \pkg{sf}.
The function automatically extracts the layer ID from the WFS URL,
assuming the URL pattern \verb{.../wfs/<layer>?REQUEST=GetCapabilities...}.
}
\examples{
\dontrun{
# Read Berlin ALKIS Bezirke directly from GetCapabilities URL
bezirke <- read_wfs_sf_auto(
  "https://gdi.berlin.de/services/wfs/alkis_bezirke?REQUEST=GetCapabilities&SERVICE=wfs"
)
plot(bezirke["bezirk"])
}

}
