% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_wfs.R
\name{wfs_base_url}
\alias{wfs_base_url}
\title{Base URL of a Berlin Geoportal WFS service}
\usage{
wfs_base_url(service, host = "https://gdi.berlin.de/services/wfs")
}
\arguments{
\item{service}{name of the service, i.e. the last path element of the WFS
endpoint (e.g. \code{"atkis"}, \code{"wsg"}). This is also the namespace prefix of
the feature types offered by that service.}

\item{host}{base path of the WFS endpoints (default:
\code{"https://gdi.berlin.de/services/wfs"}).}
}
\value{
character of length one, the service URL without query string.
Signals an error when \code{service} is not a single path element, because the
resulting URL would otherwise be handed to \code{xml2::read_xml()}, which parses
any string containing \code{<} or \code{>} as literal XML rather than fetching it.
}
\description{
The Berlin spatial data infrastructure publishes one WFS endpoint per
dataset under a common path, e.g. \verb{https://gdi.berlin.de/services/wfs/atkis}
for the ATKIS layers. This helper only composes that URL; it does not
contact the server.
}
\examples{
wfs_base_url("atkis")
}
