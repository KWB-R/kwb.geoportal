% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_wfs.R
\name{wfs_endpoint}
\alias{wfs_endpoint}
\title{Endpoint URL of a service given either way of naming it}
\usage{
wfs_endpoint(service, host = "https://gdi.berlin.de/services/wfs")
}
\arguments{
\item{service}{service name or endpoint URL.}

\item{host}{see \code{\link[=wfs_base_url]{wfs_base_url()}}.}
}
\value{
character of length one, an endpoint URL without query string.
}
\description{
\code{\link[=read_wfs]{read_wfs()}} and \code{\link[=list_wfs_layers]{list_wfs_layers()}} accept either a service name or a full
endpoint URL. The URL that \code{\link[=find_wfs]{find_wfs()}} reports carries the catalogue's own
query string -- \verb{?request=GetCapabilities&service=WFS} -- which has to go
before a new query string is appended, or the request ends up with two \code{?}
and the namespace prefix is read from the query rather than from the path.
}
\keyword{internal}
