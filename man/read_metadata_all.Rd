% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_metadata_all.R
\name{read_metadata_all}
\alias{read_metadata_all}
\title{Read all GeoNetwork / Geoportal metadata in chunks}
\usage{
read_metadata_all(
  base_url =
    "https://gdi.berlin.de/geonetwork/srv/ger/q?facet.q=type/service&resultType=details&sortBy=changeDate&fast=index",
  chunk_size = 100
)
}
\arguments{
\item{base_url}{Base GeoNetwork query URL \strong{without} \code{from} and \code{to}
parameters. Must return an XML with a \verb{<summary count="...">} node.
Defaults to the GDI Berlin service search.}

\item{chunk_size}{Number of records per request. Default: 100.}
}
\value{
A tibble with one row per metadata record, identical in structure
to the return value of \code{\link[=read_metadata]{read_metadata()}}, but for \strong{all} pages.
}
\description{
This function uses \code{\link[=read_metadata]{read_metadata()}} repeatedly to fetch \strong{all} available
metadata records from a GeoNetwork endpoint that supports \code{from} / \code{to}
pagination (like the GDI Berlin instance).
}
\details{
It first downloads the initial XML, reads the \verb{<summary count="...">}
attribute to know how many records exist, then iterates in chunks
(default: 100) until all records are read.
}
\examples{
\dontrun{
all_md <- read_metadata_all()
nrow(all_md)
}

}
\seealso{
\code{\link[=read_metadata]{read_metadata()}}
}
